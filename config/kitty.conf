include ~/.config/kitty/kitty-themes/themes/gruvbox_dark.conf

# Configuración de apariencia
font_family HackNerdFont
font_size 12
disable_ligatures never
url_color #61afef
url_style curly
background_opacity 1   
inactive_tab_background #e06c75
active_tab_background #98c379
inactive_tab_foreground #000000
tab_bar_margin_color black
# Configura el tamaño de la terminal
default_window_width 800
default_window_height 900
# Centra la ventana en la pantalla
window_center true
# Configuración de atajos de teclado personalizados
# Cargar un archivo de configuración
map ctrl+shift+f5 load_config_file
# Abrir una nueva pestaña
map ctrl+t new_tab
# (En macOS) Abrir una nueva pestaña con el emoji de manzana 🍎
map cmd+t new_tab🍎
# Cerrar la pestaña actual
map ctrl+q close_tab
# (En macOS) Cerrar la pestaña actual con el emoji de manzana 🍎
map cmd+w close_tab🍎
# Cambiar a la siguiente pestaña
map ctrl+right next_tab
# (En macOS) Cambiar a la siguiente pestaña con el emoji de manzana 🍎
map shift+cmd+] next_tab🍎
# Cambiar a la siguiente pestaña
map ctrl+tab next_tab
# Cambiar a la pestaña anterior
map ctrl+left previous_tab
# (En macOS) Cambiar a la pestaña anterior con el emoji de manzana 🍎
map shift+cmd+[ previous_tab🍎
# Cambiar a la pestaña anterior
map ctrl+shift+tab previous_tab

# Configuración del cursor
cursor_shape beam
cursor_beam_thickness 1.8

# Configuración del mouse
mause_hide_wait 3.0
# Detección de URLs
detect_urls yes
# Retraso de repintado
repaint_delay 10
# Retraso de entrada
input_delay 3
# Sincronización con el monitor
sync_to_monitor yes
# Estilo de la barra de pestañas
tab_bar_style powerline
# Configuración del shell y campana
# Configura el shell predeterminado como Zsh
Shell zsh
# Configura el comando de campana como "none" (sin campana)
bell_command none
# Deshabilita la campana de audio
enable_audio_bell no
# Habilita el control remoto de Kitty
allow_remote_control yes
# Especifica la ubicación del socket de control remoto de Kitty
listen_on unix:/tmp/kitty


